
<h1 class="text-2xl">
    Are you sure you want to delete 
    <span class="text-gray-500">App\Entity\</span><span class="underline">{{ fqcn }}</span>
    <span class="text-primary">( {{ id }} )</span> ?
</h1>
<div class="m-4 p-4">
    <p class="mb-4 text-lg">This action is irreversible.</p>
    <button type="button"
        class="hover:text-blue-800 hover:border-blue-300 py-3 px-5 text-xl hover:cursor-pointer border border-transparent bg-blue-100 rounded-2xl"
        hx-get="{{ path('autoadmin_entity_view', {'fqcn': fqcn|url_encode, 'id': id}) }}"
        hx-push-url="true" hx-swap="none">🗙 Cancel</button>
    <button type="button" 
        class="hover:text-red-800 hover:border-red-300 py-3 px-5 text-xl hover:cursor-pointer border border-transparent bg-red-100 rounded-2xl"
        hx-post="{{ path('autoadmin_entity_delete', {'fqcn': fqcn|url_encode, 'id': id}) }}"
        hx-push-url="true" hx-swap="none">🗑️ Delete</button>
</div>
